# Example for article

## TO RUN
### PHP example
`docker-compose run php  php main.php`
### To run go example
#### To build
`docker-compose run go go build main.go`
#### To run
`docker-compose run go main`